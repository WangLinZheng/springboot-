server:
  port: 8080
  servlet:
    context-path: /
spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/garbage?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.jdbc.Driver



mybatis:
  mapper-locations: classpath:mapper/**/*.xml

#打印sql时的语句
logging:
  level:
    com:
      acong:
        dao: debug
  file:
    path: G:\Eclipseidea\iworkspace\SpringbootAndMybatis\sqllog.txt
